# Mastered OAuth 2.0 and OpenID Connect (OIDC) Flows

The user demonstrated mastery of OAuth 2.0 (AuthZ access tokens) and OpenID Connect (AuthN ID tokens). They understand the four protocol roles and the PKCE exchange mechanism (Code Verifier, Code Challenge) designed to protect authorization code routing on public clients.
